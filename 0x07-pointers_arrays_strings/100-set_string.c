#include "main.h"

/**
 * set_string - sets the value of a pointer to a char datatype
 * @s: pointer to a  pointer I need to set to
 * @to: string to set
 */

void set_string(char **s, char *to)
{
	*s = to;
}
